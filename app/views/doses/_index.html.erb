<ul>
  <% @doses.each do |dose| %>
    <li>
      <%= dose.description %> of
      <%= dose.ingredient.name %>
      <%= link_to "delete this", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>
